---
phase: 03-period-filters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app/transacoes/page.tsx, src/app/dashboard/DateRangeFilter.tsx]
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "User can select period from dropdown"
    - "User can select custom date range"
    - "Selected period triggers filter callback"
  artifacts:
    - path: "src/app/transacoes/page.tsx"
      provides: "Page with DateRangeFilter integration"
      min_lines: 60
    - path: "src/components/transacoes/TransacaoList.tsx"
      provides: "List component with period prop support"
      min_lines: 40
  key_links:
    - from: "src/app/transacoes/page.tsx"
      to: "src/app/dashboard/DateRangeFilter.tsx"
      via: "import"
      pattern: "import.*DateRangeFilter"
    - from: "DateRangeFilter"
      to: "TransacaoList"
      via: "onFilterChange callback"
      pattern: "onFilterChange.*start.*end"
---

<objective>
Import and integrate DateRangeFilter component into transações page.

Purpose: Enable users to select date periods for filtering transactions
Output: DateRangeFilter rendered in transações page header
</objective>

<context>
@src/app/dashboard/DateRangeFilter.tsx (source component to reuse)
@src/app/transacoes/page.tsx (target page)
@src/components/transacoes/TransacaoList.tsx (list component)

DateRangeFilter interface:
- Props: onFilterChange(inicio: string, fim: string), className?
- Returns start/end dates in "yyyy-MM-dd" format
- Preset options: este_mes, mes_anterior, ultimos_3, ultimos_6, personalizado
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import DateRangeFilter into transações page</name>
  <files>src/app/transacoes/page.tsx</files>
  <action>
    Add import for DateRangeFilter from "@/app/dashboard/DateRangeFilter.tsx".
    Add state variables for filter dates:
    - filterStart (string, default to start of current month)
    - filterEnd (string, default to end of current month)
    
    Use date-fns to set defaults: startOfMonth(new Date()), endOfMonth(new Date())
  </action>
  <verify>TypeScript compilation passes</verify>
  <done>DateRangeFilter imported, filter state initialized with default values</done>
</task>

<task type="auto">
  <name>Task 2: Render DateRangeFilter in page header with handler</name>
  <files>src/app/transacoes/page.tsx</files>
  <action>
    Add DateRangeFilter component in the page header area (below the title).
    Add onFilterChange handler that updates both filterStart and filterEnd:
    
    ```tsx
    const handleFilterChange = (inicio: string, fim: string) => {
      setFilterStart(inicio);
      setFilterEnd(fim);
    };
    ```
    
    Pass to DateRangeFilter: onFilterChange={handleFilterChange}
  </action>
  <verify>TypeScript compilation passes, component renders</verify>
  <done>DateRangeFilter renders and updates filterStart/filterEnd on change</done>
</task>

<task type="auto">
<name>Task 3: Update TransacaoList to accept date range</name>
<files>src/components/transacoes/TransacaoList.tsx</files>
<action>
  Add optional props to TransacaoList: dateRange?: { start: string; end: string }.
  When dateRange is provided, filter items by transacao.dataHora within the range.
  Note: Transacao.dataHora is stored as ISO string, so parse and compare.
</action>
<verify>TypeScript compilation passes, filtering logic works</verify>
  <done>TransacaoList accepts dateRange and filters items</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → State | Filter state updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | date filter | accept | Client-side only, no sensitive data |
</threat_model>

<verification>
- DateRangeFilter renders in page header
- Selecting preset dates updates filter state
- Selecting custom dates updates filter state
- Filter state is passed to TransacaoList
</verification>

<success_criteria>
- User can see and interact with DateRangeFilter
- Filter callback updates state correctly
- TransacaoList receives dateRange prop
</success_criteria>

<output>
After completion, create .planning/phases/03-period-filters/03-01-SUMMARY.md
</output>