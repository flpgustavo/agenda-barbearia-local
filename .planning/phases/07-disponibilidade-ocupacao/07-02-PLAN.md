---
phase: 07-disponibilidade-ocupacao
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/app/dashboard/DisponibilidadeTab.tsx
  - src/app/dashboard/ServiceSelectorRow.tsx
  - src/app/dashboard/WeekNavigator.tsx
  - src/app/dashboard/DayColumn.tsx
  - src/app/dashboard/DisponibilidadeGrid.tsx
  - src/app/dashboard/DisponibilidadeSkeleton.tsx
  - src/app/dashboard/page.tsx
autonomous: true
requirements: [DISP-01, DISP-02, DISP-03, OCUP-01]
user_setup: []

must_haves:
  truths:
    - "Usuário vê uma barra de abas inferior no Dashboard com 'Dashboard' e 'Disponibilidade'"
    - "Usuário pode selecionar um serviço em uma row horizontal de cards"
    - "Usuário vê uma grid semanal (Seg-Dom) com colunas horizontais scrolláveis"
    - "Cada coluna de dia mostra nome do dia, data, % ocupação e chips de horários livres"
    - "Usuário pode navegar entre semanas com setas < >"
    - "Usuário pode exportar a grid como PNG via botão na barra do cabeçalho"
    - "Dias passados mostram 'Indisponível' em vez de slots"
    - "Estado de loading mostra skeletons"
    - "Estado vazio (sem serviços) mostra placeholder"
  artifacts:
    - path: "src/app/dashboard/DisponibilidadeTab.tsx"
      provides: "Container da aba de disponibilidade — orquestra ServiceSelectorRow + WeekNavigator + DisponibilidadeGrid"
    - path: "src/app/dashboard/ServiceSelectorRow.tsx"
      provides: "Row horizontal de cards de serviço com scroll"
    - path: "src/app/dashboard/WeekNavigator.tsx"
      provides: "Navegação de semanas (< >) + label 'Semana de DD/MM a DD/MM' + botão Exportar"
    - path: "src/app/dashboard/DayColumn.tsx"
      provides: "Coluna de dia individual: nome, data, % ocupação, chips de horários"
    - path: "src/app/dashboard/DisponibilidadeGrid.tsx"
      provides: "Container horizontal scrollável das 7 DayColumn"
    - path: "src/app/dashboard/DisponibilidadeSkeleton.tsx"
      provides: "Skeleton de loading para a grid (7 colunas placeholder)"
    - path: "src/app/dashboard/page.tsx"
      provides: "Modificado: bottom tab bar + conditional rendering entre Dashboard e Disponibilidade"
  key_links:
    - from: "src/app/dashboard/DisponibilidadeTab.tsx"
      to: "src/hooks/useAvailabilityGrid.ts"
      via: "import { useAvailabilityGrid }"
      pattern: "useAvailabilityGrid"
    - from: "src/app/dashboard/DisponibilidadeTab.tsx"
      to: "src/core/services/AgendamentoService.ts"
      via: "import { AgendamentoService } (para listar serviços)"
      pattern: "AgendamentoService"
    - from: "src/app/dashboard/WeekNavigator.tsx"
      to: "html-to-image"
      via: "toBlob export"
      pattern: "toBlob"
    - from: "src/app/dashboard/page.tsx"
      to: "src/app/dashboard/DisponibilidadeTab.tsx"
      via: "import + render condicional"
      pattern: "DisponibilidadeTab"
    - from: "src/app/dashboard/page.tsx"
      to: "lucide-react"
      via: "ícones da tab bar"
      pattern: "CalendarDays|BarChart3"
---

<objective>
## Interface de Usuário — Grade de Disponibilidade + Aba no Dashboard

**Propósito:** Construir todos os componentes de UI da grade semanal de disponibilidade, o seletor de serviços, o navegador de semanas, o botão de exportar PNG e a barra de abas inferior no Dashboard.

**Output:** 6 novos componentes em `src/app/dashboard/` + modificação de `page.tsx` para alternar entre Dashboard e Disponibilidade.

**Atende:** DISP-01 (selecionar serviço), DISP-02 (grid visual), DISP-03 (exportar PNG), OCUP-01 (percentual de ocupação)
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-disponibilidade-ocupacao/07-CONTEXT.md
@.planning/phases/07-disponibilidade-ocupacao/07-RESEARCH.md
@.planning/phases/07-disponibilidade-ocupacao/07-01-PLAN.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

<interfaces>
```typescript
// Interfaces do Plan 01 (já implementadas):
// src/core/services/AgendamentoService.ts
export interface DiaGrade {
  data: string;
  diaSemana: string;
  dataFormatada: string;
  slotsLivres: string[];
  totalSlots: number;
  slotsOcupados: number;
  ocupacaoPercent: number;
}

export interface GradeSemanal {
  semanaLabel: string;
  dias: DiaGrade[];
  servicoId: string;
}

// src/hooks/useAvailabilityGrid.ts
export function useAvailabilityGrid(servicoId: string | null, weekOffset: number): {
  grade: GradeSemanal | null;
  loading: boolean;
  error: string | null;
  semanaLabel: string;
}
```

```typescript
// Modelos existentes:
// src/core/models/Servico.ts
export interface Servico extends BaseModel {
    nome: string;
    duracaoMinutos: number;
    preco?: number;
}
```

```typescript
// hook de serviços existente (useServico.ts):
// Retorna { servicos: Servico[], loading, error }
// import { useServico } from "@/hooks/useServico";

// UI components disponíveis:
// import { Button } from "@/components/ui/button";
// import { Card, CardContent } from "@/components/ui/card";
// import { Skeleton } from "@/components/ui/skeleton";
// import { Separator } from "@/components/ui/separator";

// Ícones disponíveis (lucide-react):
// import { CalendarDays, BarChart3, ChevronLeft, ChevronRight, Download } from "lucide-react";

// html-to-image (já instalado):
// import { toBlob } from "html-to-image";
```

```typescript
// Estado atual de page.tsx (linhas relevantes):
export default function DashboardPage() {
    const [diaSelecionado, setDiaSelecionado] = useState(null);
    const [filters, setFilters] = useState<DashboardFilters>({...});
    const { loading, error, recarregar, receitaPorDiaSemana, topClientes, ... } = useDashboardAgendamentos(filters);

    return (
        <div className="min-h-screen bg-background text-foreground pb-20">
            <header className="sticky top-0 ...">
                {/* header com refresh + DateRangeFilter */}
            </header>
            <main className="container mx-auto p-4 space-y-6">
                {/* KPI Cards, FinancialSummaryCards, IncomeVsExpenseChart,
                    Receita por Dia da Semana, InsightsSection */}
            </main>
        </div>
    );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Criar DayColumn, DisponibilidadeGrid e DisponibilidadeSkeleton</name>
  <files>
    src/app/dashboard/DayColumn.tsx,
    src/app/dashboard/DisponibilidadeGrid.tsx,
    src/app/dashboard/DisponibilidadeSkeleton.tsx
  </files>

  <read_first>
  - src/app/dashboard/page.tsx (padrão de componentes co-localizados no dashboard)
  - src/core/services/AgendamentoService.ts (interface DiaGrade — após Plan 01)
  - src/components/ui/skeleton.tsx (padrão de skeleton)
  - src/components/ui/card.tsx (padrão de card)
  - src/components/ui/button.tsx (padrão de button outline/ghost)
  </read_first>

  <action>
  **Arquivo 1: `src/app/dashboard/DayColumn.tsx`**

  ```tsx
  "use client";

  import type { DiaGrade } from "@/core/services/AgendamentoService";

  interface DayColumnProps {
    dia: DiaGrade;
    isPast: boolean;
  }

  export function DayColumn({ dia, isPast }: DayColumnProps) {
    const ocup = dia.ocupacaoPercent;
    const barColor =
      ocup > 75 ? "bg-red-500" : ocup > 50 ? "bg-amber-500" : "bg-emerald-500";

    return (
      <div className={`flex flex-col w-[130px] shrink-0 border rounded-lg p-3 bg-card ${isPast ? "opacity-50" : ""}`}>
        {/* Cabeçalho: nome do dia + data */}
        <div className="text-center mb-2">
          <p className="text-xs font-bold text-muted-foreground uppercase">{dia.diaSemana}</p>
          <p className="text-sm font-semibold">{dia.dataFormatada}</p>
        </div>

        {/* Indicador de ocupação (D-09) */}
        <div className="mb-2">
          <div className="flex items-center justify-between text-[10px] text-muted-foreground mb-1">
            <span>Ocupação</span>
            <span className="font-semibold">{ocup}%</span>
          </div>
          <div className="h-1.5 w-full bg-muted rounded-full overflow-hidden">
            <div
              className={`h-full rounded-full transition-all ${barColor}`}
              style={{ width: `${Math.min(ocup, 100)}%` }}
            />
          </div>
        </div>

        <div className="border-t pt-2" />

        {/* Chips de horários livres (D-05) */}
        <div className="flex flex-wrap gap-1">
          {dia.slotsLivres.length > 0 ? (
            dia.slotsLivres.map((slot) => (
              <span
                key={slot}
                className="inline-flex items-center px-2 py-0.5 rounded-full text-xs border bg-background text-foreground"
              >
                {slot}
              </span>
            ))
          ) : (
            <p className="text-[10px] text-muted-foreground italic w-full text-center py-2">
              {isPast ? "Indisponível" : "Lotado"}
            </p>
          )}
        </div>
      </div>
    );
  }
  ```

  **Arquivo 2: `src/app/dashboard/DisponibilidadeGrid.tsx`**

  ```tsx
  "use client";

  import { useRef } from "react";
  import type { GradeSemanal } from "@/core/services/AgendamentoService";
  import { DayColumn } from "./DayColumn";

  interface DisponibilidadeGridProps {
    grade: GradeSemanal;
    exportRef: React.RefObject<HTMLDivElement | null>;
    isExporting: boolean;
  }

  export function DisponibilidadeGrid({ grade, exportRef, isExporting }: DisponibilidadeGridProps) {
    const hoje = new Date();
    const hojeStr = hoje.toISOString().split("T")[0];

    return (
      <div ref={exportRef}>
        <div className={`overflow-x-auto pb-4 ${isExporting ? "min-w-[980px]" : ""}`}>
          <div className="flex gap-3 min-w-max">
            {grade.dias.map((dia) => (
              <DayColumn
                key={dia.data}
                dia={dia}
                isPast={dia.data < hojeStr}
              />
            ))}
          </div>
        </div>
      </div>
    );
  }
  ```

  **Arquivo 3: `src/app/dashboard/DisponibilidadeSkeleton.tsx`**

  ```tsx
  "use client";

  import { Skeleton } from "@/components/ui/skeleton";

  export function DisponibilidadeSkeleton() {
    return (
      <div className="overflow-x-auto pb-4">
        <div className="flex gap-3 min-w-max">
          {Array.from({ length: 7 }).map((_, i) => (
            <div key={i} className="flex flex-col w-[130px] shrink-0 border rounded-lg p-3 bg-card">
              <Skeleton className="h-4 w-16 mx-auto mb-1" />
              <Skeleton className="h-5 w-14 mx-auto mb-3" />
              <Skeleton className="h-4 w-full mb-1" />
              <div className="border-t pt-2 mt-1">
                <Skeleton className="h-6 w-14 mb-1" />
                <Skeleton className="h-6 w-14 mb-1" />
                <Skeleton className="h-6 w-10" />
              </div>
            </div>
          ))}
        </div>
      </div>
    );
  }
  ```

  **Observações:**
  - Todos os componentes usam `"use client"` (padrão Next.js para interatividade)
  - `DayColumn`: prop `isPast` controla opacidade e texto de indisponível (dias passados)
  - Barra de ocupação usa cores semafóricas: verde (<50%), ambar (50-75%), vermelho (>75%)
  - Chips seguem o padrão existente no `AgendamentoFormDrawer.tsx` (rounded-full, text-xs, border)
  - `DisponibilidadeGrid` aceita `exportRef` para captura do `html-to-image`
  - `isExporting` força `min-w-[980px]` para capturar todas as 7 colunas no PNG (Pitfall 2)
  - `DisponibilidadeSkeleton` renderiza 7 colunas placeholder com `Skeleton` components
  </action>

  <verify>
  <automated>cd "$PROJECT_ROOT" && npx tsc --noEmit --pretty 2>&1 | Select-String -Pattern "error" -NotMatch | Select-Object -First 1</automated>
  </verify>

  <acceptance_criteria>
  - `src/app/dashboard/DayColumn.tsx` existe e exporta `function DayColumn({ dia, isPast })`
  - `src/app/dashboard/DisponibilidadeGrid.tsx` existe e exporta `function DisponibilidadeGrid({ grade, exportRef, isExporting })`
  - `src/app/dashboard/DisponibilidadeSkeleton.tsx` existe e exporta `function DisponibilidadeSkeleton()`
  - `DayColumn` usa `ocupacaoPercent` para cor da barra e percentual
  - `DayColumn` renderiza chips com `rounded-full text-xs border`
  - `DisponibilidadeGrid` aplica `min-w-[980px]` quando `isExporting` é true
  - `npx tsc --noEmit` passa sem erros
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Tarefa 2: Criar ServiceSelectorRow, WeekNavigator e DisponibilidadeTab</name>
  <files>
    src/app/dashboard/ServiceSelectorRow.tsx,
    src/app/dashboard/WeekNavigator.tsx,
    src/app/dashboard/DisponibilidadeTab.tsx
  </files>

  <read_first>
  - src/app/dashboard/DisponibilidadeGrid.tsx (criado na Tarefa 1)
  - src/app/dashboard/DisponibilidadeSkeleton.tsx (criado na Tarefa 1)
  - src/hooks/useAvailabilityGrid.ts (hook que consome — criado no Plan 01)
  - src/core/services/AgendamentoService.ts (interface GradeSemanal — Plan 01)
  - src/app/dashboard/page.tsx (padrão de importação de hooks)
  - src/app/agendamentos/AgendamentoFormDrawer.tsx (padrão toBlob export — linhas 94-137)
  </read_first>

  <action>
  **Arquivo 1: `src/app/dashboard/ServiceSelectorRow.tsx`**

  ```tsx
  "use client";

  import type { Servico } from "@/core/models/Servico";

  interface ServiceSelectorRowProps {
    servicos: Servico[];
    servicoId: string | null;
    onSelect: (id: string) => void;
  }

  export function ServiceSelectorRow({ servicos, servicoId, onSelect }: ServiceSelectorRowProps) {
    if (servicos.length === 0) {
      return (
        <div className="text-center py-8 text-muted-foreground">
          <p className="text-sm">Nenhum serviço cadastrado.</p>
          <p className="text-xs">Cadastre serviços em Serviços para ver a disponibilidade.</p>
        </div>
      );
    }

    return (
      <div className="overflow-x-auto pb-2">
        <div className="flex gap-2 min-w-max">
          {servicos.map((servico) => (
            <button
              key={servico.id}
              onClick={() => onSelect(servico.id!)}
              className={`
                shrink-0 px-4 py-2 rounded-lg border text-sm font-medium transition-colors
                ${servicoId === servico.id
                  ? "bg-primary text-primary-foreground border-primary"
                  : "bg-card text-card-foreground border-border hover:bg-accent"
                }
              `}
            >
              <span>{servico.nome}</span>
              {servico.preco != null && (
                <span className="ml-2 text-xs opacity-70">
                  {new Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL" }).format(servico.preco)}
                </span>
              )}
            </button>
          ))}
        </div>
      </div>
    );
  }
  ```

  **Arquivo 2: `src/app/dashboard/WeekNavigator.tsx`**

  ```tsx
  "use client";

  import { ChevronLeft, ChevronRight, Download } from "lucide-react";
  import { Button } from "@/components/ui/button";

  interface WeekNavigatorProps {
    semanaLabel: string;
    onPrevWeek: () => void;
    onNextWeek: () => void;
    onExport: () => void;
    isExporting: boolean;
  }

  export function WeekNavigator({ semanaLabel, onPrevWeek, onNextWeek, onExport, isExporting }: WeekNavigatorProps) {
    return (
      <div className="flex items-center justify-between mb-4">
        <div className="flex items-center gap-2">
          <Button variant="outline" size="icon" onClick={onPrevWeek} className="h-8 w-8">
            <ChevronLeft className="h-4 w-4" />
          </Button>
          <span className="text-sm font-semibold whitespace-nowrap">{semanaLabel}</span>
          <Button variant="outline" size="icon" onClick={onNextWeek} className="h-8 w-8">
            <ChevronRight className="h-4 w-4" />
          </Button>
        </div>
        <Button
          variant="outline"
          size="sm"
          onClick={onExport}
          disabled={isExporting}
          className="gap-1"
        >
          <Download className="h-4 w-4" />
          <span className="hidden sm:inline">Exportar</span>
        </Button>
      </div>
    );
  }
  ```

  **Arquivo 3: `src/app/dashboard/DisponibilidadeTab.tsx`**

  ```tsx
  "use client";

  import { useState, useRef, useCallback } from "react";
  import { useServico } from "@/hooks/useServico";
  import { useAvailabilityGrid } from "@/hooks/useAvailabilityGrid";
  import { toBlob } from "html-to-image";
  import { ServiceSelectorRow } from "./ServiceSelectorRow";
  import { WeekNavigator } from "./WeekNavigator";
  import { DisponibilidadeGrid } from "./DisponibilidadeGrid";
  import { DisponibilidadeSkeleton } from "./DisponibilidadeSkeleton";
  import { toast } from "sonner";

  export function DisponibilidadeTab() {
    const [servicoId, setServicoId] = useState<string | null>(null);
    const [weekOffset, setWeekOffset] = useState(0);
    const [isExporting, setIsExporting] = useState(false);
    const exportRef = useRef<HTMLDivElement>(null);

    const { servicos, loading: loadingServicos } = useServico();
    const { grade, loading, error, semanaLabel } = useAvailabilityGrid(servicoId, weekOffset);

    // Auto-select first service when services load and none selected (D-11)
    // Disparado também quando serviço muda (D-07: auto-refresh)
    const handleServiceSelect = useCallback((id: string) => {
      setServicoId(id);
    }, []);

    const handlePrevWeek = () => setWeekOffset((prev) => prev - 1);
    const handleNextWeek = () => setWeekOffset((prev) => prev + 1);

    const handleExport = async () => {
      if (!exportRef.current) return;
      setIsExporting(true);

      try {
        const blob = await toBlob(exportRef.current, {
          cacheBust: true,
          backgroundColor: `hsl(${getComputedStyle(document.documentElement)
            .getPropertyValue("--card")})`,
          pixelRatio: 2,
        });

        if (!blob) throw new Error("Falha ao gerar imagem.");

        const url = URL.createObjectURL(blob);
        const link = document.createElement("a");
        link.download = `disponibilidade-${semanaLabel.replace(/\//g, "-")}.png`;
        link.href = url;
        link.click();
        URL.revokeObjectURL(url);

        toast.success("Imagem exportada com sucesso!");
      } catch (error) {
        console.error("Erro ao exportar:", error);
        toast.error("Erro ao exportar a imagem.");
      } finally {
        setIsExporting(false);
      }
    };

    // Estado vazio: sem serviços cadastrados
    if (!loadingServicos && servicos.length === 0) {
      return (
        <div className="p-4">
          <ServiceSelectorRow servicos={[]} servicoId={null} onSelect={() => {}} />
        </div>
      );
    }

    // Estado: nenhum serviço selecionado ainda
    if (!servicoId && !loadingServicos) {
      return (
        <div className="p-4 space-y-4">
          <ServiceSelectorRow
            servicos={servicos}
            servicoId={null}
            onSelect={handleServiceSelect}
          />
          <div className="flex items-center justify-center py-16 text-muted-foreground">
            <p className="text-sm">Selecione um serviço para ver a disponibilidade</p>
          </div>
        </div>
      );
    }

    return (
      <div className="p-4 space-y-4">
        {/* Seletor de serviço (D-11) */}
        <ServiceSelectorRow
          servicos={servicos}
          servicoId={servicoId}
          onSelect={handleServiceSelect}
        />

        {/* Barra de navegação + export (D-06, D-12) */}
        <WeekNavigator
          semanaLabel={grade?.semanaLabel ?? semanaLabel}
          onPrevWeek={handlePrevWeek}
          onNextWeek={handleNextWeek}
          onExport={handleExport}
          isExporting={isExporting}
        />

        {/* Grid de disponibilidade ou loading ou erro */}
        {error && (
          <div className="p-4 rounded bg-destructive/15 text-destructive text-sm font-medium">
            {error}
          </div>
        )}

        {loading ? (
          <DisponibilidadeSkeleton />
        ) : grade ? (
          <DisponibilidadeGrid
            grade={grade}
            exportRef={exportRef}
            isExporting={isExporting}
          />
        ) : null}
      </div>
    );
  }
  ```

  **Observações de design (D-01 a D-12):**
  - `ServiceSelectorRow`: scroll horizontal com cards de serviço. Card selecionado fica `bg-primary text-primary-foreground`. Mostra nome + preço formatado em BRL.
  - `WeekNavigator`: setas < > + label "Semana de DD/MM a DD/MM" (D-08). Botão Exportar com ícone Download (D-12).
  - `DisponibilidadeTab`: orquestrador que integra todos os subcomponentes + hook.
  - O export usa `toBlob` com `cacheBust: true`, `pixelRatio: 2` e fallback de download (não share — conforme recomendação da research, Open Question #3).
  - Estado vazio (sem serviços): mostra mensagem do ServiceSelectorRow + "Selecione um serviço" no centro.
  - Estado de erro: barra vermelha estilo destrutivo (mesmo padrão do page.tsx).
  - `useServico` hook já existe no projeto (`src/hooks/useServico.ts`).
  </action>

  <verify>
  <automated>cd "$PROJECT_ROOT" && npx tsc --noEmit --pretty 2>&1 | Select-String -Pattern "error" -NotMatch | Select-Object -First 1</automated>
  </verify>

  <acceptance_criteria>
  - `src/app/dashboard/ServiceSelectorRow.tsx` existe com `export function ServiceSelectorRow`
  - `src/app/dashboard/WeekNavigator.tsx` existe com `export function WeekNavigator`
  - `src/app/dashboard/DisponibilidadeTab.tsx` existe com `export function DisponibilidadeTab`
  - `ServiceSelectorRow` renderiza cards clicáveis com nome e preço do serviço
  - `WeekNavigator` contém botões ChevronLeft, ChevronRight, Download e label de semana
  - `DisponibilidadeTab` importa e usa `useAvailabilityGrid` e `useServico`
  - `DisponibilidadeTab` importa `toBlob` de `html-to-image`
  - `DisponibilidadeTab` tem estados: loading (skeleton), erro (barra vermelha), sem serviço (placeholder), vazio (sem serviços)
  - `npx tsc --noEmit` passa sem erros
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Tarefa 3: Modificar page.tsx — adicionar bottom tab bar + rendering condicional</name>
  <files>src/app/dashboard/page.tsx</files>

  <read_first>
  - src/app/dashboard/page.tsx (estado atual completo — ~249 linhas)
  - src/app/dashboard/DisponibilidadeTab.tsx (componente a ser importado — criado na Tarefa 2)
  </read_first>

  <action>
  Modificar `src/app/dashboard/page.tsx` para adicionar:

  **1. Import do novo componente e ícone:**
  Adicionar ao bloco de imports:
  ```tsx
  import { DisponibilidadeTab } from "./DisponibilidadeTab";
  import { CalendarDays, BarChart3 } from "lucide-react";
  ```

  O import de `lucide-react` já existe com `RefreshCw, TrendingUp, Users, Clock, Wallet`. Trocar para:
  ```tsx
  import {
      RefreshCw,
      TrendingUp,
      Users,
      Clock,
      Wallet,
      CalendarDays,
      BarChart3,
  } from "lucide-react";
  ```

  **2. Estado activeTab:**
  Adicionar após `const [diaSelecionado, setDiaSelecionado] = useState(null);`:
  ```tsx
  const [activeTab, setActiveTab] = useState<"dashboard" | "disponibilidade">("dashboard");
  ```

  **3. No JSX, substituir TODO o conteúdo dentro do `<div className="min-h-screen...">`:**

  O div principal deve conter:
  ```tsx
  <div className="min-h-screen bg-background text-foreground pb-24">
      {/* Header — sempre visível, mesmo na aba Disponibilidade */}
      <header className="sticky top-0 z-10 border-b bg-background/95 backdrop-blur supports-backdrop-filter:bg-background/60">
          <div className="container mx-auto p-4 flex items-center justify-between">
              <div className="flex items-center gap-3">
                  <Button
                      size="icon"
                      variant="ghost"
                      onClick={() => recarregar()}
                      disabled={loading}
                  >
                      <RefreshCw className={`h-5 w-5 ${loading ? "animate-spin" : ""}`} />
                  </Button>
                  <div>
                      <h1 className="text-xl font-bold tracking-tight">Dashboard</h1>
                      <p className="text-xs text-muted-foreground">
                          {activeTab === "dashboard"
                              ? "Visão geral do negócio"
                              : "Disponibilidade semanal"}
                      </p>
                  </div>
              </div>
          </div>

          {/* Barra de Filtros — só aparece na aba Dashboard (filtro de período não se aplica à grade semanal) */}
          {activeTab === "dashboard" && (
              <div className="container mx-auto px-4 pb-4">
                  <DateRangeFilter onFilterChange={handleFilterChange} />
              </div>
          )}
      </header>

      {/* Conteúdo condicional (D-01: state-based tab switch) */}
      {activeTab === "dashboard" ? (
          <main className="container mx-auto p-4 space-y-6">
              {error && (
                  <div className="p-4 rounded bg-destructive/15 text-destructive text-sm font-medium">
                      {error}
                  </div>
              )}

              {/* 1. KPI Cards (Resumo) */}
              <section className="grid grid-cols-2 gap-3 md:grid-cols-4">
                  <KPICard
                      title="Dia Campeão"
                      value={loading ? "..." : receitaPorDiaSemana.diaCampeao.dia}
                      subValue={loading ? "" : formatCurrency(receitaPorDiaSemana.diaCampeao.totalReceita)}
                      icon={<TrendingUp className="h-4 w-4 text-primary" />}
                      loading={loading}
                  />
                  <KPICard
                      title="Ticket Médio"
                      value={loading ? "..." : formatCurrency(receitaPorDiaSemana.diaCampeao.ticketMedio)}
                      subValue="No melhor dia"
                      icon={<Wallet className="h-4 w-4 text-emerald-500" />}
                      loading={loading}
                  />
                  <KPICard
                      title="Retorno Médio"
                      value={loading ? "..." : `${Math.round(frequenciaRetorno.mediaDias)} dias`}
                      subValue="Frequência"
                      icon={<Clock className="h-4 w-4 text-blue-500" />}
                      loading={loading}
                  />
                  <KPICard
                      title="Agendamentos"
                      value={loading ? "..." : `${agendamentos.length}`}
                      subValue={"Total em aberto: " + agendamentos.filter(a => a.status == "CONFIRMADO").length}
                      icon={<Users className="h-4 w-4 text-primary" />}
                      loading={loading}
                  />
              </section>

              {/* Phase 5 — Métricas Financeiras */}
              <section className="space-y-4">
                  <Separator className="my-2" />
                  <FinancialSummaryCards
                      receitaTotal={receitaTotal}
                      despesaTotal={despesaTotal}
                      saldo={saldo}
                      loading={loading}
                  />
                  <IncomeVsExpenseChart
                      receitaTotal={receitaTotal}
                      despesaTotal={despesaTotal}
                      loading={loading}
                  />
              </section>

              {/* 2. Receita Semanal (Gráfico de Barras CSS) */}
              <Card onClick={() => setDiaSelecionado(null)}>
                  <CardHeader className="pb-2">
                      <CardTitle className="text-base font-semibold">Receita por Dia da Semana</CardTitle>
                      <CardDescription>Toque na barra para ver o valor</CardDescription>
                  </CardHeader>
                  <CardContent>
                      {loading ? (
                          <Skeleton className="h-40 w-full" />
                      ) : (
                          <div className="flex items-end justify-between gap-1 h-52 mt-6 px-1">
                              {receitaPorDiaSemana.porDia.map((item) => {
                                  const percent = (item.totalReceita / (receitaPorDiaSemana.potencialMaximoDia || 1)) * 100;
                                  const isBest = item.dia === receitaPorDiaSemana.diaCampeao.dia;
                                  const isSelected = diaSelecionado === item.dia;
                                  const diaAbreviado = item.dia.substring(0, 3).toUpperCase();

                                  return (
                                      <div
                                          key={item.dia}
                                          className="flex flex-col items-center justify-end w-full relative h-full max-w-[45px] cursor-pointer"
                                          onClick={(e) => {
                                              e.stopPropagation();
                                              handleBarClick(item.dia);
                                          }}
                                      >
                                          <div className={`absolute -top-10 text-white dark:text-black bg-accent-foreground text-sm py-1 px-2 rounded shadow-lg transition-all duration-200 pointer-events-none ${isSelected ? 'opacity-100 scale-100' : 'opacity-0 scale-95'}`}>
                                              {formatCurrency(item.totalReceita)}
                                              <div className="absolute -bottom-1 left-1/2 -translate-x-1/2 w-2 h-2 bg-accent-foreground rotate-45"></div>
                                          </div>

                                          <div
                                              className={`w-full rounded-t-sm transition-all duration-300 ${isSelected ? 'ring-1 ring-offset-1 ring-slate-400' : ''} ${isBest ? 'bg-primary' : 'bg-primary/25'}`}
                                              style={{ height: `${Math.max(percent, 4)}%` }}
                                          ></div>

                                          <span className={`text-[12px] mt-2 font-bold transition-colors ${isSelected || isBest ? 'text-primary' : 'text-muted-foreground'}`}>
                                              {diaAbreviado}
                                          </span>
                                      </div>
                                  );
                              })}
                          </div>
                      )}
                  </CardContent>
              </Card>

              {/* Phase 6 — Rankings & Insights */}
              <InsightsSection
                  servicosRanking={servicosRanking}
                  topServices12meses={topServices12meses}
                  topClientes12meses={topClientes12meses}
                  ultimaVisitaPorCliente={ultimaVisitaPorCliente}
                  loading={loading}
              />
          </main>
      ) : (
          <DisponibilidadeTab />
      )}

      {/* Bottom Tab Bar (D-01) */}
      <div className="fixed bottom-0 left-0 right-0 z-50 border-t bg-background">
          <div className="container mx-auto flex justify-around py-2">
              <button
                  onClick={() => setActiveTab("dashboard")}
                  className={`flex flex-col items-center gap-1 px-6 py-1 text-xs transition-colors ${
                      activeTab === "dashboard"
                          ? "text-primary"
                          : "text-muted-foreground"
                  }`}
              >
                  <BarChart3 className="h-5 w-5" />
                  <span>Dashboard</span>
              </button>
              <button
                  onClick={() => setActiveTab("disponibilidade")}
                  className={`flex flex-col items-center gap-1 px-6 py-1 text-xs transition-colors ${
                      activeTab === "disponibilidade"
                          ? "text-primary"
                          : "text-muted-foreground"
                  }`}
              >
                  <CalendarDays className="h-5 w-5" />
                  <span>Disponibilidade</span>
              </button>
          </div>
      </div>
  </div>
  ```

  **O que NÃO muda:**
  - O hook `useDashboardAgendamentos(filters)` permanece (só é usado na aba Dashboard)
  - `handleFilterChange`, `handleBarClick` permanecem
  - `KPICard` subcomponente permanece
  - `pb-20` → `pb-24` para acomodar a tab bar fixa (Pitfall 5)
  - O header é compartilhado entre as abas, mas o DateRangeFilter só aparece na aba Dashboard
  - O subtítulo do header muda conforme a aba ativa
  </action>

  <verify>
  <automated>cd "$PROJECT_ROOT" && npx tsc --noEmit --pretty 2>&1 | Select-String -Pattern "error" -NotMatch | Select-Object -First 1</automated>
  </verify>

  <acceptance_criteria>
  - `src/app/dashboard/page.tsx` importa `DisponibilidadeTab` de `"./DisponibilidadeTab"`
  - `src/app/dashboard/page.tsx` importa `CalendarDays, BarChart3` de `lucide-react`
  - `page.tsx` contém `const [activeTab, setActiveTab] = useState<"dashboard" | "disponibilidade">("dashboard")`
  - `page.tsx` contém `<DisponibilidadeTab />` dentro do bloco `activeTab === "disponibilidade"`
  - `page.tsx` contém uma `<div className="fixed bottom-0...">` com dois botões de aba (Dashboard + Disponibilidade)
  - O ícone `CalendarDays` está no botão "Disponibilidade" e `BarChart3` no botão "Dashboard"
  - `pb-20` foi alterado para `pb-24` no container principal
  - `npx tsc --noEmit` passa sem erros
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side components ← IndexedDB | Dados da grade vêm do hook useAvailabilityGrid que consulta IndexedDB via service layer |
| html-to-image export ← DOM | Export lê o DOM renderizado para gerar PNG — sem acesso externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Denial of Service | DisponibilidadeGrid | accept | Grade com muitos slots (expediente longo + serviço curto) pode renderizar centenas de chips. Scroll horizontal + virtualização não necessária para volume esperado (barbeiro autônomo, ~50 slots/dia). |
| T-07-04 | Information Disclosure | Export PNG | accept | Export contém dados que o usuário já vê na tela. Sem exfiltração de dados adicionais. |
</threat_model>

<verification>
1. ✅ `npx tsc --noEmit` passa sem erros
2. ✅ Dashboard renderiza com header + bottom tab bar + conteúdo condicional
3. ✅ Aba "Dashboard" mostra conteúdo original (KPI, finanças, insights)
4. ✅ Aba "Disponibilidade" mostra ServiceSelectorRow → WeekNavigator → DisponibilidadeGrid
5. ✅ Dias passados aparecem com opacidade reduzida e "Indisponível"
6. ✅ Export gera PNG com todas as 7 colunas visíveis
</verification>

<success_criteria>
- Bottom tab bar funcional com 2 abas: Dashboard e Disponibilidade
- ServiceSelectorRow mostra serviços cadastrados em scroll horizontal
- Grade semanal renderiza 7 colunas (Seg-Dom) com chips de horários livres e % ocupação
- Navegação entre semanas com setas < >
- Export PNG via html-to-image com download do arquivo
- Estados de loading (skeleton), erro, vazio (sem serviços), sem seleção tratados
- TypeScript compila sem erros
</success_criteria>

<output>
After completion, create `.planning/phases/07-disponibilidade-ocupacao/07-02-SUMMARY.md`
</output>
