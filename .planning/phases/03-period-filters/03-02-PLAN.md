---
phase: 03-period-filters
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/app/transacoes/page.tsx, src/components/transacoes/TransacaoList.tsx]
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Transaction list shows only transactions within selected period"
    - "Totals are calculated only for filtered transactions"
    - "Empty state shows when no transactions in period"
  artifacts:
    - path: "src/app/transacoes/page.tsx"
      provides: "Page passing dateRange to TransacaoList"
    - path: "src/components/transacoes/TransacaoList.tsx"
      provides: "Filtered list with recalculated totals"
  key_links:
    - from: "page.tsx"
      to: "TransacaoList"
      via: "dateRange prop"
---

<objective>
Connect the date filter to transactions list, enabling period-based filtering and total recalculation.

Purpose: Make filtering actually work - show filtered transactions and update totals
Output: Transactions filtered by date, totals recalculated per period
</objective>

<context>
@src/app/transacoes/page.tsx
@src/components/transacoes/TransacaoList.tsx
@src/core/models/Transacao.ts (for dataHora field)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pass dateRange prop to TransacaoList</name>
  <files>src/app/transacoes/page.tsx</files>
  <action>
    Add a dateRange state (if not already in Plan 01):
    const [dateRange, setDateRange] = useState({ start: filterStart, end: filterEnd });
    
    Update handleFilterChange to also update dateRange:
    const handleFilterChange = (inicio: string, fim: string) => {
      setFilterStart(inicio);
      setFilterEnd(fim);
      setDateRange({ start: inicio, end: fim });
    };
    
    Pass to TransacaoList: dateRange={dateRange}
  </action>
  <verify>TypeScript compilation passes, dateRange object constructed from two strings</verify>
  <done>dateRange={{ start: filterStart, end: filterEnd }} prop passed to TransacaoList</done>
</task>

<task type="auto">
  <name>Task 2: Filter transactions by date range</name>
  <files>src/components/transacoes/TransacaoList.tsx</files>
  <action>
    Add dateRange prop interface to TransacaoListProps.
    Implement filtering logic:
    - If dateRange is provided, filter items where:
      - Parse transacao.dataHora (ISO string) to Date
      - Check if date >= start and date <= end
    
    Note: Compare dates at start of day for start date, end of day for end date.
    Use date-fns parseISO and comparison functions.
  </action>
  <verify>TypeScript compilation passes, filtering works correctly</verify>
  <done>Transactions filtered by selected period</done>
</task>

<task type="auto">
<name>Task 3: Recalculate totals based on filtered items</name>
<files>src/components/transacoes/TransacaoList.tsx</files>
  <action>
    Calculate totals from the filtered items array (not rawItems).
    This ensures totals reflect only transactions within the selected period.
    
    Current logic already uses `items` for totals, so ensure the filtering
    happens before the totals calculation in the render.
  </action>
  <verify>Totals update when date range changes</verify>
  <done>Totals show correct values for selected period</done>
</task>

<task type="auto">
  <name>Task 4: Show contextual empty state when filter returns no results</name>
  <files>src/components/transacoes/TransacaoList.tsx</files>
  <action>
    Update the empty state in TransacaoList to show contextual suggestions when dateRange is active.
    
    Split the empty state logic into two branches:
    - If no items AND no active filter: show "Nenhuma transação encontrada" + "Toque no botão + para criar sua primeira transação" (original message)
    - If no items AND active filter: show "Nenhuma transação neste período" + "Tente selecionar outro período" (D-03-01)
    
    Implementation approach: check if dateRange is provided and has valid dates. If items.length === 0 AND dateRange exists, show the filter-aware message. Otherwise fall back to the original message.
  </action>
  <verify>TypeScript compilation passes, filtered empty state shows contextual message</verify>
  <done>Filtered empty state shows "Nenhuma transação neste período" with suggestion per D-03-01</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → Logic | Date filtering logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Denial of Service | large date range | accept | Client-side, limited data |
</threat_model>

<verification>
- Selecting "Este mês" filters to current month transactions only
- Selecting "Mês anterior" filters to last month transactions
- Totals show sum of filtered transactions only
- Empty state shows when no transactions in range
</verification>

<success_criteria>
- Transactions are correctly filtered by period
- Totals reflect only filtered transactions
- Empty state displays appropriately
</success_criteria>

<output>
After completion, create .planning/phases/03-period-filters/03-02-SUMMARY.md
</output>