---
phase: 02-mobile-transactions
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - src/app/transacoes/page.tsx
autonomous: true
requirements: [RF2.1, RF2.3, RNF2.1]
user_setup: []
must_haves:
  truths:
    - "Page displays transactions using TransacaoList component"
    - "User can click item to edit via form drawer"
    - "User can delete transaction"
    - "FAB (Floating Action Button) remains for creating new transactions"
    - "Total values shown in header"
  artifacts:
    - path: "src/app/transacoes/page.tsx"
      provides: "Transacoes page with TransacaoList integration"
      min_lines: 100
  key_links:
    - from: "src/app/transacoes/page.tsx"
      to: "TransacaoList"
      via: "import and replace Table"
    - from: "TransacaoList"
      to: "TransacaoListItem"
      via: "internal component usage"
---
<objective>
Replace Table component with TransacaoList in the transactions page while preserving existing functionality.

Purpose: Enable mobile-friendly transaction display on the main page
Output: Updated transacoes/page.tsx using TransacaoList
</objective>

<context>
@src/app/transacoes/page.tsx (current page with Table - needs updating)
@src/components/transacoes/TransacaoList.tsx (from plan 02-01)
@src/components/transacoes/TransacaoListItem.tsx (from plan 02-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate TransacaoList into page</name>
  <files>src/app/transacoes/page.tsx</files>
  <action>
    Update transacoes/page.tsx:
    - Remove Table imports (Table, TableBody, TableCaption, TableCell, TableFooter, TableHead, TableHeader, TableRow)
    - Add TransacaoList import: import { TransacaoList } from "@/components/transacoes/TransacaoList"
    - Replace Table JSX with TransacaoList component:
      - Pass items prop: items={items}
      - Pass onEdit handler: onEdit={(transacao) => handleForm(transacao)}
      - Pass onDelete handler: onDelete={handleDelete}
    - Keep FAB (Floating Action Button) for creating new transactions
    - Keep header with h1 and description
    - Remove TableFooter totals (move to TransacaoList header if needed)
    - Preserve all existing state and handlers
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Page renders TransacaoList instead of Table, edit/delete still work</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → page | onEdit, onDelete handlers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Repudiation | delete action | accept | Local-only app, no audit needed |
</threat_model>

<verification>
- Page loads without errors
- Clicking edit opens drawer
- Clicking delete removes transaction
- FAB creates new transaction
</verification>

<success_criteria>
- TransacaoList replaces Table component
- Edit functionality preserved
- Delete functionality preserved
- FAB preserved
</success_criteria>

<output>
After completion, create .planning/phases/02-mobile-transactions/02-02-SUMMARY.md
</output>