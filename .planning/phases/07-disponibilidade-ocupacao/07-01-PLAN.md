---
phase: 07-disponibilidade-ocupacao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/services/AgendamentoService.ts
  - src/lib/queryKeys.ts
  - src/hooks/useAvailabilityGrid.ts
autonomous: true
requirements: [DISP-01, DISP-02, DISP-03, OCUP-01]
user_setup: []

must_haves:
  truths:
    - "O método gerarGradeSemanal(servicoId) computa 7 dias em uma única query no IndexedDB"
    - "O retorno de gerarGradeSemanal inclui slotsLivres, ocupacaoPercent e totalSlots por dia"
    - "useAvailabilityGrid retorna grade, loading, error e semanaLabel via React Query"
    - "O queryKey gradeDisponibilidade invalida corretamente quando servicoId ou semana muda"
  artifacts:
    - path: "src/core/services/AgendamentoService.ts"
      provides: "Novo método gerarGradeSemanal + interfaces GradeSemanal/DiaGrade"
      exports: ["GradeSemanal", "DiaGrade", "AgendamentoService.gerarGradeSemanal"]
    - path: "src/lib/queryKeys.ts"
      provides: "Query key gradeDisponibilidade para cache do React Query"
      contains: "gradeDisponibilidade"
    - path: "src/hooks/useAvailabilityGrid.ts"
      provides: "Hook useAvailabilityGrid que consome gerarGradeSemanal via useQuery"
      exports: ["useAvailabilityGrid"]
  key_links:
    - from: "src/hooks/useAvailabilityGrid.ts"
      to: "src/core/services/AgendamentoService.ts"
      via: "import { AgendamentoService, GradeSemanal }"
      pattern: "AgendamentoService\\.gerarGradeSemanal"
    - from: "src/hooks/useAvailabilityGrid.ts"
      to: "src/lib/queryKeys.ts"
      via: "import { queryKeys }"
      pattern: "queryKeys\\.gradeDisponibilidade"
    - from: "src/core/services/AgendamentoService.ts"
      to: "db.agendamentos.where().between()"
      via: "Dexie ranged query (7 dias em 1 pass)"
      pattern: "between\\(startISO"
---

<objective>
## Camada de Dados — Grade Semanal de Disponibilidade

**Propósito:** Criar o método `gerarGradeSemanal(servicoId)` no `AgendamentoService` que computa a grade de disponibilidade de 7 dias (segunda a domingo) em uma única query no IndexedDB, mais o hook `useAvailabilityGrid` e a query key correspondente.

**Output:** Interfaces `GradeSemanal`/`DiaGrade` exportadas, método `gerarGradeSemanal` no service, query key `gradeDisponibilidade` em `queryKeys.ts`, hook `useAvailabilityGrid` em `src/hooks/`.

**Atende:** DISP-01 (selecionar serviço → ver grade), DISP-02 (grid visual com slots livres), OCUP-01 (percentual de ocupação por dia)
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-disponibilidade-ocupacao/07-CONTEXT.md
@.planning/phases/07-disponibilidade-ocupacao/07-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

<interfaces>
```typescript
// Modelos existentes relevantes:
// src/core/models/Servico.ts
export interface Servico extends BaseModel {
    nome: string;
    duracaoMinutos: number;   // ← usado para tamanho do slot
    preco?: number;
}

// src/core/models/Usuario.ts
export interface Usuario extends BaseModel {
    nome: string;
    inicio: string;           // "08:00" — início expediente
    fim: string;              // "19:00" — fim expediente
    intervaloInicio: string;  // "12:00" — início intervalo
    intervaloFim: string;     // "13:00" — fim intervalo
}

// src/core/models/Agendamento.ts
export type AgendamentoStatus = 'CONCLUIDO' | 'CONFIRMADO' | 'CANCELADO';
export interface Agendamento extends BaseModel {
    clienteId: string;
    dataHora: string;         // "2026-05-19T10:00:00"
    servicoId: string;
    status: AgendamentoStatus;
    observacoes?: string;
}
```

```typescript
// Método existente que servirá de referência (AgendamentoService.ts:229-295):
async gerarHorariosDisponiveis(dataStr: string, duracaoMinutos: number, passoMinutos = 30): Promise<string[]>
// Lógica: itera de inicioExpediente até fimExpediente com passo=passoMinutos,
// verifica intervalo e conflitos com agendamentos, retorna string[] de horários livres.
// IMPORTANTE: o novo método NÃO usa passo — passo = duracaoMinutos sempre (D-03).

// Singleton pattern (AgendamentoService.ts:319):
export const AgendamentoService = new AgendamentoServiceClass();
```

```typescript
// queryKeys existente (src/lib/queryKeys.ts):
export const queryKeys = {
  clientes: ['clientes'] as const,
  servicos: ['servicos'] as const,
  usuarios: ['usuarios'] as const,
  transacoes: ['transacoes'] as const,
  agendamentos: ['agendamentos'] as const,
  agendamentosDetalhes: ['agendamentos', 'detalhes'] as const,
  agendamentoDetalhe: (id: string) => ['agendamentos', 'detalhe', id] as const,
};
```

```typescript
// Helper privado existente em AgendamentoService:
private toMinutes(hora: string): number {
    const [h, m] = hora.split(":").map(Number);
    return h * 60 + m;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Adicionar interfaces GradeSemanal/DiaGrade + método gerarGradeSemanal</name>
  <files>src/core/services/AgendamentoService.ts</files>

  <read_first>
  - src/core/services/AgendamentoService.ts (estado atual completo)
  - src/core/models/Servico.ts (duracaoMinutos)
  - src/core/models/Usuario.ts (inicio, fim, intervaloInicio, intervaloFim)
  - src/core/models/Agendamento.ts (dataHora, status, servicoId)
  </read_first>

  <action>
  Adicionar no arquivo `src/core/services/AgendamentoService.ts`:

  **1. Interfaces exportadas** (adicionar ANTES da classe, após os imports):

  ```typescript
  export interface DiaGrade {
    data: string;            // "2026-05-19"
    diaSemana: string;       // "SEG", "TER", "QUA", "QUI", "SEX", "SAB", "DOM"
    dataFormatada: string;   // "19/05"
    slotsLivres: string[];   // ["08:00", "08:30", ...]
    totalSlots: number;
    slotsOcupados: number;
    ocupacaoPercent: number; // 0-100 (arredondado)
  }

  export interface GradeSemanal {
    semanaLabel: string;     // "Semana de 19/05 a 25/05"
    dias: DiaGrade[];
    servicoId: string;
  }
  ```

  **2. Método público `gerarGradeSemanal`** (adicionar DENTRO da classe `AgendamentoServiceClass`, após o método `gerarHorariosDisponiveis`):

  ```typescript
  async gerarGradeSemanal(servicoId: string, weekStart?: Date): Promise<GradeSemanal> {
    const usuario = await db.usuarios.toCollection().first();
    if (!usuario) return { semanaLabel: "", dias: [], servicoId };

    const servico = await db.servicos.get(servicoId);
    if (!servico) return { semanaLabel: "", dias: [], servicoId };

    const start = weekStart ?? startOfWeek(new Date(), { weekStartsOn: 1 }); // Segunda-feira
    const end = endOfWeek(start, { weekStartsOn: 1 }); // Domingo
    const days = eachDayOfInterval({ start, end });

    const inicioExpediente = this.toMinutes(usuario.inicio);
    const fimExpediente = this.toMinutes(usuario.fim);
    const intervaloInicio = usuario.intervaloInicio ? this.toMinutes(usuario.intervaloInicio) : null;
    const intervaloFim = usuario.intervaloFim ? this.toMinutes(usuario.intervaloFim) : null;
    const duracao = servico.duracaoMinutos;
    const passo = duracao; // D-03: passo = duração do serviço (não 30min fixo)

    // Query ÚNICA para todos os 7 dias (D-10: evitar 7 queries individuais)
    const startISO = format(start, "yyyy-MM-dd");
    const endISO = format(end, "yyyy-MM-dd");
    const todosAgendamentos = await db.agendamentos
      .where("dataHora")
      .between(startISO, `${endISO}T23:59:59`, true, true)
      .toArray();

    const hoje = new Date();
    const hojeStr = format(hoje, "yyyy-MM-dd");
    const agoraMin = hoje.getHours() * 60 + hoje.getMinutes();

    const dias: DiaGrade[] = days.map((dia) => {
      const dataStr = format(dia, "yyyy-MM-dd");
      const diaSemana = format(dia, "EEE", { locale: ptBR }).toUpperCase().replace(".", "").substring(0, 3);
      const dataFormatada = format(dia, "dd/MM");

      // Filtra agendamentos do dia (já em memória — sem nova query)
      const agendamentosDoDia = todosAgendamentos.filter(
        (ag) => ag.dataHora.startsWith(dataStr) && ag.status !== "CANCELADO"
      );

      // Pré-computa intervalos dos agendamentos com duração do serviço atual
      const intervalosAgendamento = agendamentosDoDia.map((ag) => ({
        inicio: this.toMinutes(ag.dataHora.slice(11, 16)),
        fim: this.toMinutes(ag.dataHora.slice(11, 16)) + duracao,
      }));

      const slotsLivres: string[] = [];
      let slotsOcupados = 0;
      let totalSlots = 0;

      for (let tempo = inicioExpediente; tempo + duracao <= fimExpediente; tempo += passo) {
        const inicioSlot = tempo;
        const fimSlot = tempo + duracao;

        // Pula horários passados se for hoje
        if (dataStr === hojeStr && inicioSlot < agoraMin) continue;

        let conflito = false;

        // Verifica intervalo (almoço)
        if (intervaloInicio !== null && intervaloFim !== null &&
            inicioSlot < intervaloFim && fimSlot > intervaloInicio) {
          conflito = true;
        }

        // Verifica conflito com agendamentos
        if (!conflito) {
          for (const ag of intervalosAgendamento) {
            if (inicioSlot < ag.fim && fimSlot > ag.inicio) {
              conflito = true;
              break;
            }
          }
        }

        totalSlots++;
        if (conflito) {
          slotsOcupados++;
        } else {
          const h = Math.floor(tempo / 60).toString().padStart(2, "0");
          const m = (tempo % 60).toString().padStart(2, "0");
          slotsLivres.push(`${h}:${m}`);
        }
      }

      return {
        data: dataStr,
        diaSemana,
        dataFormatada,
        slotsLivres,
        totalSlots,
        slotsOcupados,
        ocupacaoPercent: totalSlots > 0 ? Math.round((slotsOcupados / totalSlots) * 100) : 0,
      };
    });

    const semanaLabel = `Semana de ${format(start, "dd/MM")} a ${format(end, "dd/MM")}`;
    return { semanaLabel, dias, servicoId };
  }
  ```

  **3. Imports necessários** (adicionar ao cabeçalho de imports existente):
  ```typescript
  import { startOfWeek, endOfWeek, eachDayOfInterval, format } from "date-fns";
  import { ptBR } from "date-fns/locale";
  ```

  **Observações importantes:**
  - NÃO modifique nenhum método existente — apenas adicione as interfaces e o novo método
  - O método usa `passo = duracao` (conforme D-03: slot = duração do serviço, não 30min fixo)
  - A query usa `.between(startISO, endISO)` em vez de `.toArray()` — usa o índice `dataHoa` do Dexie (Pitfall 1 da research)
  - O cálculo de ocupação subtrai slots de intervalo do denominador (Pitfall 4)
  - A verificação de dia atual pula horários já passados (consistente com `gerarHorariosDisponiveis`)
  </action>

  <verify>
  <automated>cd "$PROJECT_ROOT" && npx tsc --noEmit --pretty 2>&1 | Select-String -Pattern "error" -NotMatch | Select-Object -First 1</automated>
  </verify>

  <acceptance_criteria>
  - `src/core/services/AgendamentoService.ts` contém `export interface DiaGrade {` com os campos: data, diaSemana, dataFormatada, slotsLivres, totalSlots, slotsOcupados, ocupacaoPercent
  - `src/core/services/AgendamentoService.ts` contém `export interface GradeSemanal {` com os campos: semanaLabel, dias, servicoId
  - `AgendamentoServiceClass` contém `async gerarGradeSemanal(servicoId: string, weekStart?: Date): Promise<GradeSemanal>`
  - O método usa `db.agendamentos.where("dataHora").between(...)` — contém `.between(`
  - `import { startOfWeek, endOfWeek, eachDayOfInterval, format } from "date-fns"` no cabeçalho
  - `import { ptBR } from "date-fns/locale"` no cabeçalho
  - `npx tsc --noEmit` passa sem erros
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Tarefa 2: Adicionar query key + criar hook useAvailabilityGrid</name>
  <files>src/lib/queryKeys.ts, src/hooks/useAvailabilityGrid.ts</files>

  <read_first>
  - src/lib/queryKeys.ts (estado atual)
  - src/hooks/useDashboardAgendamentos.ts (padrão de hook com useQuery)
  - src/core/services/AgendamentoService.ts (após Tarefa 1 — contém GradeSemanal e gerarGradeSemanal)
  </read_first>

  <action>
  **Arquivo 1: `src/lib/queryKeys.ts`**

  Adicionar ao objeto `queryKeys`:
  ```typescript
  gradeDisponibilidade: ['gradeDisponibilidade'] as const,
  ```

  O objeto completo deve ficar:
  ```typescript
  export const queryKeys = {
    clientes: ['clientes'] as const,
    servicos: ['servicos'] as const,
    usuarios: ['usuarios'] as const,
    transacoes: ['transacoes'] as const,
    agendamentos: ['agendamentos'] as const,
    agendamentosDetalhes: ['agendamentos', 'detalhes'] as const,
    agendamentoDetalhe: (id: string) => ['agendamentos', 'detalhe', id] as const,
    gradeDisponibilidade: ['gradeDisponibilidade'] as const,
  };
  ```

  **Arquivo 2: `src/hooks/useAvailabilityGrid.ts`** (CRIAR novo arquivo)

  ```typescript
  "use client";

  import { useQuery } from "@tanstack/react-query";
  import { AgendamentoService, GradeSemanal } from "@/core/services/AgendamentoService";
  import { queryKeys } from "@/lib/queryKeys";
  import { startOfWeek, addWeeks } from "date-fns";

  export function useAvailabilityGrid(servicoId: string | null, weekOffset: number) {
    const weekStart = startOfWeek(addWeeks(new Date(), weekOffset), { weekStartsOn: 1 });

    const { data, isLoading, error } = useQuery<GradeSemanal>({
      queryKey: [...queryKeys.gradeDisponibilidade, servicoId, weekStart.toISOString()],
      queryFn: () => AgendamentoService.gerarGradeSemanal(servicoId!, weekStart),
      enabled: !!servicoId,
    });

    return {
      grade: data ?? null,
      loading: isLoading,
      error: error ? (error as Error).message : null,
      semanaLabel: data?.semanaLabel ?? "",
    };
  }
  ```

  **Observações:**
  - O hook usa `"use client"` pois é um hook de dados que será usado em componente cliente
  - Segue o mesmo padrão de `useDashboardAgendamentos.ts` (useQuery com queryKeys)
  - Retorna `grade` (pode ser null), `loading`, `error` (string | null), `semanaLabel`
  - Só executa a query quando `servicoId` não é null (prop `enabled: !!servicoId`)
  - O queryKey depende de `[servicoId, weekStart.toISOString()]` — quando qualquer um muda, refetch automático (D-07)
  </action>

  <verify>
  <automated>cd "$PROJECT_ROOT" && npx tsc --noEmit --pretty 2>&1 | Select-String -Pattern "error" -NotMatch | Select-Object -First 1</automated>
  </verify>

  <acceptance_criteria>
  - `src/lib/queryKeys.ts` contém `gradeDisponibilidade: ['gradeDisponibilidade'] as const`
  - `src/hooks/useAvailabilityGrid.ts` existe e contém `export function useAvailabilityGrid`
  - `useAvailabilityGrid` usa `useQuery<GradeSemanal>({...})` com queryKey contendo `...queryKeys.gradeDisponibilidade, servicoId, weekStart.toISOString()`
  - `useAvailabilityGrid` retorna `{ grade, loading, error, semanaLabel }`
  - `npx tsc --noEmit` passa sem erros
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side (browser) ↔ IndexedDB | Toda a computação é local no navegador. Sem API externa, sem servidor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | IndexedDB (db.agendamentos) | accept | App offline-first sem servidor. Usuário só manipula seus próprios dados no navegador. Risco aceito por design. |
| T-07-02 | Information Disclosure | html-to-image export | accept | Export gera PNG do DOM renderizado — só contém dados que o usuário já está vendo na tela. Sem dados adicionais vazados. |
</threat_model>

<verification>
1. ✅ `npx tsc --noEmit` passa sem erros de tipo
2. ✅ `GradeSemanal` e `DiaGrade` são exportadas do service
3. ✅ `useAvailabilityGrid` é importável e retorna a estrutura esperada
</verification>

<success_criteria>
- `gerarGradeSemanal(servicoId)` existe e retorna `GradeSemanal` com 7 `DiaGrade`
- A query no IndexedDB usa `.between()` com intervalo de 7 dias (única query)
- `queryKeys.gradeDisponibilidade` existe em `queryKeys.ts`
- `useAvailabilityGrid(servicoId, weekOffset)` compila e retorna `{ grade, loading, error, semanaLabel }`
- O hook só executa quando `servicoId` é fornecido
</success_criteria>

<output>
After completion, create `.planning/phases/07-disponibilidade-ocupacao/07-01-SUMMARY.md`
</output>
