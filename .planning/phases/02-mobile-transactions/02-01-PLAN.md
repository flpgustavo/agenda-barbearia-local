---
phase: 02-mobile-transactions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/transacoes/TransacaoListItem.tsx
  - src/components/transacoes/TransacaoList.tsx
autonomous: true
requirements: [RF2.1, RF2.2, RNF2.1]
user_setup: []
must_haves:
  truths:
    - "User sees transactions in responsive card list layout"
    - "Each item shows date, description, status badge, and value with color coding"
    - "List adapts from mobile (cards) to desktop (table-like)"
    - "Touch targets are at least 44px"
  artifacts:
    - path: "src/components/transacoes/TransacaoListItem.tsx"
      provides: "Individual transaction card component"
      min_lines: 50
    - path: "src/components/transacoes/TransacaoList.tsx"
      provides: "Container component with responsive layout"
      min_lines: 40
  key_links:
    - from: "src/components/transacoes/TransacaoList.tsx"
      to: "TransacaoListItem"
      via: "import and map items"
    - from: "TransacaoListItem"
      to: "@/components/ui/card"
      via: "Card, CardContent imports"
    - from: "TransacaoListItem"
      to: "@/components/ui/badge"
      via: "Badge import for status"
---

<objective>
Create responsive transaction list components that display transactions in a mobile-first card layout, replacing the current Table component.

Purpose: Enable users to view and manage transactions on mobile devices with touch-friendly UI
Output: TransacaoListItem and TransacaoList components
</objective>

<context>
@src/core/models/Transacao.ts (Transacao interface: id, dataHora, tipo, status, valor, observacoes, agendamentoId)
@src/components/ui/card.tsx (Card, CardContent exports)
@src/components/ui/badge.tsx (Badge, badgeVariants exports)
@src/components/ui/button.tsx (Button exports)
@src/app/transacoes/page.tsx (current implementation with Table)
</context>

<interfaces>
<!-- Key interfaces needed from codebase -->

From src/core/models/Transacao.ts:
```typescript
export type TransacaoStatus = 'AGENDADO' | 'CONFIRMADO' | 'CANCELADO';
export interface Transacao extends BaseModel {
    dataHora: string;
    agendamentoId?: string;
    tipo: 'ENTRADA' | 'SAIDA';
    status: TransacaoStatus;
    valor: number;
    observacoes?: string;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create TransacaoListItem component</name>
  <files>src/components/transacoes/TransacaoListItem.tsx</files>
  <action>
    Create mobile-first transaction card component with:
    - Props interface: TransacaoListItemProps with transacao, onClick, onDelete
    - Date display: Format dataHora to dd/MM using toLocaleDateString
    - Description: Show observacoes or "Sem descrição"
    - Status badge: Use Badge component with variant based on status
      - AGENDADO: variant="outline" with yellow tones
      - CONFIRMADO: variant="outline" with green tones  
      - CANCELADO: variant="destructive" or outline with red tones
    - Value display: Green text for ENTRADA, red for SAIDA (use text-green-600 / text-red-600)
    - Appointment indicator: Show "Agendamento" badge/label when agendamentoId exists
    - Click handler: Call onClick with transacao for edit
    - Delete button: Call onDelete with transacao.id
    - Touch target: Ensure minimum 44px height for clickable areas
    - Use cn() utility for conditional classes
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>TransacaoListItem renders correctly with all fields visible</done>
</task>

<task type="auto">
  <name>Task 2: Create TransacaoList container component</name>
  <files>src/components/transacoes/TransacaoList.tsx</files>
  <action>
    Create responsive list container with:
    - Props interface: TransacaoListProps with items (Transacao[]), onEdit, onDelete
    - Mobile layout (default): Display as vertical stack of TransacaoListItem cards
    - Desktop layout (640px+): Display as table-like layout using grid
    - Header row: Show "Data", "Descrição", "Situação", "Valor" labels in desktop view
    - Empty state: Show friendly message when items.length === 0
    - Map items to TransacaoListItem components
    - Use Tailwind responsive classes: grid-cols-1 (default), md:grid-cols-4 (desktop)
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>TransacaoList renders items in responsive layout adapting at 640px breakpoint</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → component | transacao data from Dexie (trusted local DB) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | TransacaoListItem | accept | Data from local IndexedDB, no external input |
| T-02-02 | Info Disclosure | display | accept | No sensitive data exposure beyond local user |
</threat_model>

<verification>
- Components compile without TypeScript errors
- Responsive breakpoint works at 640px (sm: breakpoint)
</verification>

<success_criteria>
- TransacaoListItem shows: date (dd/MM), description, status badge, value with color, agendamento indicator
- TransacaoList adapts: card stack on mobile, table-like on desktop
- Touch targets ≥44px
</success_criteria>

<output>
After completion, create .planning/phases/02-mobile-transactions/02-01-SUMMARY.md
</output>